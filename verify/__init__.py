"""
verify/ — validation and evaluation modules.

Imported by the test suite and by the judges endpoints, so proof in CI and
proof in a browser cannot drift apart (CLAUDE.md, "Write the validation once").
"""
